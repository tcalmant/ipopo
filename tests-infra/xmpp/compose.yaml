services:
  xmpp:
    image: ejabberd/ecs:latest
    ports:
      - "5222:5222"
    environment:
      - ERL_OPTS=-noshell
    volumes:
      - ./register_users.sh:/opt/register_users.sh:ro
    post_start:
      - command: /bin/sh /opt/register_users.sh
